apply plugin: "com.android.application"
apply plugin: "org.jetbrains.kotlin.android"
apply plugin: "com.facebook.react"

configurations.all {
    resolutionStrategy {
        force 'org.jetbrains.kotlin:kotlin-stdlib:2.0.21'
        force 'org.jetbrains.kotlin:kotlin-stdlib-jdk8:2.0.21'
    }
}

android {
    ndkVersion "26.1.10909125"  // 🔥 நேரடியாக கொடுக்கப்பட்டுள்ளது
    buildToolsVersion "34.0.0"   // 🔥 நேரடியாக கொடுக்கப்பட்டுள்ளது
    compileSdk 34                // 🔥 compileSdk பிழைக்கான தீர்வு

    namespace "com.autopartsindia"
    defaultConfig {
        applicationId "com.autopartsindia"
        minSdkVersion 24
        targetSdkVersion 34      // 🔥 நேரடியாக கொடுக்கப்பட்டுள்ளது
        versionCode 1
        versionName "1.0.0"
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
    buildFeatures {
        buildConfig true
    }

    signingConfigs {
        debug {
            if (file('debug.keystore').exists()) {
                storeFile file('debug.keystore')
                storePassword 'android'
                keyAlias 'androiddebugkey'
                keyPassword 'android'
            }
        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            signingConfig signingConfigs.debug
            minifyEnabled false
            shrinkResources false
        }
    }

    packaging {
        resources {
            excludes += [
                "META-INF/DEPENDENCIES",
                "META-INF/LICENSE",
                "META-INF/LICENSE.txt",
                "META-INF/NOTICE",
                "META-INF/NOTICE.txt",
                "META-INF/*.kotlin_module"
            ]
        }
    }
}

dependencies {
    implementation("com.facebook.react:react-android:+")
    implementation platform("com.google.firebase:firebase-bom:33.7.0")
    implementation("com.google.firebase:firebase-analytics")
    implementation("com.google.firebase:firebase-messaging")
}

apply plugin: "com.google.gms.google-services"
